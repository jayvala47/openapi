paths:
  /pet:
    post:
      summary: Add a new pet to the store
      x-swagger-router-controller: SampleController
      description: ""
      operationId: addPet
      consumes:
      produces:
      parameters:
          schema:
            ref: novalue 
